# Graph Architectures

Combined home for graph-native models:

- `Supervised/` and `Self-Supervised/` graph suites
- `Autoencoder/` for patch-graph autoencoders
- `DAE/` for node-feature graph denoising autoencoders

Fully connected models that consume graph inputs remain under `MLPS/graph/`.
