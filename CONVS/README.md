# Convolutional Architectures

Combined home for convolution-backed families:

- `CNN/`
- `Autoencoder/`
- `DAE/`
- `VAE/`

Each source family retains its original training-paradigm layout so base models,
ADP wrappers, and runners remain adjacent.
