# requirements.txt
# Keep PyPI as the main index for non-torch deps, and use the CPU PyTorch
# wheel index for a faster, compatible setup.
--index-url https://download.pytorch.org/whl/cpu

torch
torchvision
torchaudio

# useful companions (CPU libs, fine with CUDA PyTorch)
numpy
scipy
matplotlib
pandas
scikit-learn
tqdm
Pillow
pycocotools
nltk
