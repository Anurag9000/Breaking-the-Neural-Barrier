# requirements.txt
# Keep PyPI as the main index for non-torch deps, and use the PyTorch CUDA wheel index for GPU builds.
--extra-index-url https://download.pytorch.org/whl/cu124

torch
torchvision
torchaudio

# useful companions (CPU libs, fine with CUDA PyTorch)
numpy
scipy
matplotlib
pandas
scikit-learn
tqdm
Pillow
