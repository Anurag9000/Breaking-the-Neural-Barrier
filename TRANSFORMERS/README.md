# Transformer Architectures

Combined home for attention-backed families:

- `Transformer/` for the broad ViT, language, and sequence collection
- `Autoencoder/` for transformer autoencoders
- `DAE/` for MAE-ViT and token-mask denoising autoencoders
