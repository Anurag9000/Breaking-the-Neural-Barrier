# Denoising archive

This task-first tree is derived from the historical small STL archive.

Modes:
- `w2d/`: archived ADP width-to-depth rows
- `width_only/`: width-only records when recovered or linked from adjacent historical runs
- `stl_ablation/`: fixed-grid STL ablation rows
