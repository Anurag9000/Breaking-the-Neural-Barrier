# Run Split Plan

The current split plan, canonical task list, and resume policy are maintained
in [docs/tabular_dae_dnn/README.md](../../../../docs/tabular_dae_dnn/README.md).

This file remains only as a pointer so the repo keeps a single authoritative
experiment layout reference.
